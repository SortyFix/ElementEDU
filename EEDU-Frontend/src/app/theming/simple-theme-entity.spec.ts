import { SimpleThemeEntity } from './simple-theme-entity';

describe('SimpleThemeEntity', () => {
  it('should create an instance', () => {
    expect(new SimpleThemeEntity()).toBeTruthy();
  });
});
